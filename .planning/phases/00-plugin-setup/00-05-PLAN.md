---
phase: "00-plugin-setup"
plan: "05"
type: execute
wave: 3
depends_on:
  - "00-02"
  - "00-03"
  - "00-04"
files_modified:
  - README.md
autonomous: false
requirements:
  - INSTALL-01
  - INSTALL-02
  - INSTALL-03

must_haves:
  truths:
    - "sf log --help lists all 5 subcommands: search, trace, download, purge, filter"
    - "sf log search --help shows summary and --target-org flag"
    - "sf log filter --help shows summary and --keyword flag (no --target-org)"
    - "npm test exits 0 (all unit tests pass)"
    - "node -e 'require(\"./package.json\")' && node --version shows >=18.0.0 compatibility"
    - "README.md documents installation, requirements, and available commands"
  artifacts:
    - path: "README.md"
      provides: "Plugin documentation with installation instructions, Node.js requirement, and command list"
      contains: "sf plugin install"
    - path: "lib/index.js"
      provides: "Compiled plugin entry point"
    - path: "lib/commands/log/search.js"
      provides: "Compiled search command"
    - path: "lib/commands/log/filter.js"
      provides: "Compiled filter command"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "Installation instructions reference package name"
      pattern: "sf plugin install"
    - from: "sf log --help"
      to: "lib/commands/log/"
      via: "oclif command discovery from compiled lib/"
      pattern: "search|trace|download|purge|filter"
---

<objective>
Verify the complete Phase 0 installation and write the README documenting plugin usage.
This is the phase gate checkpoint — human confirms all 4 INSTALL requirements are met
before Phase 1 begins.

Purpose: Plans 01-04 build the foundation. This plan verifies the complete integrated system
works: compile → link → commands register → tests pass → Node 18 constraint enforced.
README documents the installation path required by INSTALL-01.

Output:
- README.md with installation, requirements, and command reference
- Human confirmation that sf log --help works and lists all 5 commands
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/PROJECT.md
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/00-plugin-setup/00-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/00-plugin-setup/00-RESEARCH.md
@/Users/mc/Repos/sf-local-logs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with installation instructions and command reference</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/00-plugin-setup/00-CONTEXT.md — all locked decisions for command names
    - .planning/REQUIREMENTS.md — INSTALL-01, INSTALL-02, INSTALL-03 full text
    - .planning/ROADMAP.md — Phase goals and success criteria for all phases
    - package.json (from Plan 01) — package name, version, engines field
  </read_first>
  <action>
    Write README.md at the project root. The README serves as the primary documentation
    for INSTALL-01 (plugin is installable via sf plugin install).

    Structure:
    1. Title: "sf-local-logs" with short description from package.json
    2. Installation section:
       - Prerequisites: Node.js 18.0.0+ (link to nodejs.org), Salesforce CLI v2.x+ (link to install guide)
       - Install command: sf plugin install <github-repo-url>
       - Local development: sf plugins link . --no-verify
    3. Requirements section: List INSTALL-01, INSTALL-02, INSTALL-03 in plain English
    4. Commands section: Table with all 5 commands, their namespace, and status
       | Command | Namespace | Status | Phase |
       |---------|-----------|--------|-------|
       | Search users | sf log search | Planned | Phase 1 |
       | Trace session | sf log trace | Planned | Phase 2 |
       | Download logs | sf log download | Planned | Phase 3 |
       | Purge logs | sf log purge | Planned | Phase 3 |
       | Filter logs | sf log filter | Planned | Phase 4 |
    5. Development section:
       - npm run setup-devorg — sets up scratch org and links plugin
       - npm run compile — compile TypeScript
       - npm test — run unit tests
       - npm run test:nuts — run integration tests (requires DevHub)
    6. License: MIT

    For the installation URL in step 2, use the git remote origin URL. Check with:
      git remote get-url origin

    If the remote URL is an SSH URL (git@github.com:...), convert to HTTPS format for the README.

    Keep README concise — avoid lengthy explanations. Admins and engineers need installation
    steps and command reference, not architecture documentation.
  </action>
  <verify>
    <automated>grep -c "sf plugin install" README.md && grep -c ">=18.0.0\|18.0.0+" README.md && grep -c "sf log search\|sf log trace\|sf log download\|sf log purge\|sf log filter" README.md</automated>
  </verify>
  <done>
    README.md exists at project root.
    Contains "sf plugin install" in installation instructions.
    Contains "18.0.0" or ">=18.0.0" in prerequisites (Node.js requirement).
    Contains all 5 command names (sf log search, trace, download, purge, filter).
    File is at least 50 lines with clear section headers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full integration verification — compile, link, test, help output</name>
  <files></files>
  <read_first>
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Validation Architecture: Sampling Rate section
    - .planning/ROADMAP.md — Phase 0 Success Criteria (all 4 must be TRUE)
  </read_first>
  <action>
    Run the complete Phase 0 verification sequence. This task produces no files — it runs
    verification commands and collects results.

    Verification sequence:
    1. Clean compile from scratch:
         npm run compile
       Verify lib/commands/log/ exists with 5 .js files.

    2. Re-link the plugin:
         sf plugins link . --no-verify
       Verify no errors.

    3. Verify Node.js engines constraint in package.json:
         node -e "const p = require('./package.json'); console.log('engines:', JSON.stringify(p.engines))"
       Expected output: engines: {"node":">=18.0.0"}

    4. Check plugin registration:
         sf log --help
       Verify output lists: search, trace, download, purge, filter as subcommands.

    5. Verify --target-org flag on org-dependent commands:
         sf log search --help | grep -i "target-org"
         sf log trace --help | grep -i "target-org"
         sf log download --help | grep -i "target-org"
         sf log purge --help | grep -i "target-org"

    6. Verify filter command does NOT require --target-org (local file operation):
         sf log filter --help | grep -i "keyword"
       Verify --keyword flag appears.

    7. Run all unit tests:
         npm test
       Verify "passing" in output, no failures.

    8. Verify Node.js version compatibility message from sf --version:
         sf --version
       Confirm sf CLI v2.x+ is what's running.

    Document all outputs. If any step fails, investigate and fix before marking done.

    If sf log --help does not show all 5 commands, the most likely causes are:
    - lib/commands/log/*.js not compiled (run npm run compile again)
    - Plugin not linked (run sf plugins link . --no-verify)
    - oclif.commands in package.json points to wrong path

    Fix root cause, do not work around. All 5 commands must appear in sf log --help.
  </action>
  <verify>
    <automated>sf log --help 2>&1 | grep -E "search|trace|download|purge|filter" | wc -l</automated>
  </verify>
  <done>
    sf log --help output contains all 5 subcommands.
    npm test exits 0 with all tests passing.
    package.json engines.node is ">=18.0.0".
    sf CLI version is 2.x+.
    lib/commands/log/ contains search.js, trace.js, download.js, purge.js, filter.js.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-05 have built the complete Phase 0 plugin scaffold:
    - package.json with engines constraint (Node >=18.0.0) and oclif config
    - TypeScript compiler config (tsconfig.json) targeting lib/
    - ESLint, Mocha, Prettier, coverage configs
    - 5 placeholder command files (src/commands/log/*.ts) using SfCommand
    - 5 messages files (messages/log.*.md) with help text
    - 10 test files (5 unit .test.ts + 5 integration .nut.ts)
    - GitHub Actions workflows (lint.yml, build.yml, test.yml)
    - Dev setup script (scripts/setup-devorg.sh)
    - README.md with installation instructions
    - Plugin compiled to lib/ and linked via sf plugins link
  </what-built>
  <how-to-verify>
    Run these checks in your terminal:

    1. Check all 5 commands register:
         sf log --help
       Expected: Lists search, trace, download, purge, filter as subcommands

    2. Check a command's help (with --target-org flag):
         sf log search --help
       Expected: Shows summary "Search for Salesforce users by name" and --target-org flag

    3. Check filter command (no --target-org, has --keyword):
         sf log filter --help
       Expected: Shows --keyword flag, no --target-org flag

    4. Check Node.js engines constraint:
         node -e "console.log(require('./package.json').engines)"
       Expected: { node: '>=18.0.0' }

    5. Run unit tests:
         npm test
       Expected: All tests pass (10 assertions across 5 command test files)

    6. Check CI/CD workflows exist:
         ls .github/workflows/
       Expected: lint.yml, build.yml, test.yml

    7. Verify Phase 0 success criteria from ROADMAP.md are all TRUE:
       a. Plugin can be installed via sf plugin install — framework ready (sf plugins link works)
       b. Plugin registers all commands — sf log --help shows 5 commands
       c. Node.js 18+ enforced — package.json engines field present
       d. Works with sf CLI v2.x+ — sf CLI 2.61.8 loaded plugin successfully
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 checks pass.
    Or describe any failures so they can be investigated and fixed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sf log --help → user terminal | Output displayed to developer; no untrusted input at verification stage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Information Disclosure | README.md repository URL | accept | README is public documentation; repository URL is intentionally public for sf plugin install |
| T-00-13 | Tampering | sf plugins link local path | accept | Local development link only; limited to developer's machine; not production path |
| T-00-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs; verification uses already-installed packages from Plan 01 |
</threat_model>

<verification>
Phase 0 complete when checkpoint is approved with all checks passing:
1. sf log --help — all 5 subcommands listed
2. sf log search --help — --target-org flag visible, summary shown
3. sf log filter --help — --keyword flag visible, no --target-org
4. package.json engines.node — ">=18.0.0"
5. npm test — 0 failures
6. ls .github/workflows/ — 3 workflow files present
7. Human checkpoint: "approved"
</verification>

<success_criteria>
- README.md documents sf plugin install path (INSTALL-01)
- sf log --help shows all 5 subcommands (INSTALL-02 — CLI v2 registers plugin correctly)
- engines.node ">=18.0.0" in package.json (INSTALL-03)
- npm test passes (regression guard for Phase 1+)
- Human checkpoint approved — developer has visually confirmed all 4 ROADMAP success criteria
</success_criteria>

<output>
Create `.planning/phases/00-plugin-setup/00-05-SUMMARY.md` when done
</output>
