---
phase: "00-plugin-setup"
plan: "02"
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - src/commands/log/search.ts
  - src/commands/log/trace.ts
  - src/commands/log/download.ts
  - src/commands/log/purge.ts
  - src/commands/log/filter.ts
  - messages/log.search.md
  - messages/log.trace.md
  - messages/log.download.md
  - messages/log.purge.md
  - messages/log.filter.md
autonomous: true
requirements:
  - INSTALL-01
  - INSTALL-02
  - UX-04
  - UX-05

must_haves:
  truths:
    - "sf log search --help shows summary text and --target-org flag"
    - "sf log trace --help shows summary text and --target-org flag"
    - "sf log download --help shows summary text and --target-org flag"
    - "sf log purge --help shows summary text and --target-org flag"
    - "sf log filter --help shows summary text and no --target-org (local operation)"
    - "All 5 commands compile without TypeScript errors"
    - "npm run compile exits 0 after adding command files"
  artifacts:
    - path: "src/commands/log/search.ts"
      provides: "Placeholder search command extending SfCommand"
      contains: "SfCommand"
      exports: ["Search"]
    - path: "src/commands/log/trace.ts"
      provides: "Placeholder trace command extending SfCommand"
      contains: "SfCommand"
      exports: ["Trace"]
    - path: "src/commands/log/download.ts"
      provides: "Placeholder download command extending SfCommand"
      contains: "SfCommand"
      exports: ["Download"]
    - path: "src/commands/log/purge.ts"
      provides: "Placeholder purge command extending SfCommand"
      contains: "SfCommand"
      exports: ["Purge"]
    - path: "src/commands/log/filter.ts"
      provides: "Placeholder filter command extending SfCommand"
      contains: "SfCommand"
      exports: ["Filter"]
    - path: "messages/log.search.md"
      provides: "Help text for search command"
      contains: "summary"
    - path: "messages/log.trace.md"
      provides: "Help text for trace command"
      contains: "summary"
    - path: "messages/log.download.md"
      provides: "Help text for download command"
      contains: "summary"
    - path: "messages/log.purge.md"
      provides: "Help text for purge command"
      contains: "summary"
    - path: "messages/log.filter.md"
      provides: "Help text for filter command"
      contains: "summary"
  key_links:
    - from: "src/commands/log/search.ts"
      to: "messages/log.search.md"
      via: "Messages.loadMessages('sf-local-logs', 'log.search')"
      pattern: "loadMessages.*log\\.search"
    - from: "src/commands/log/*.ts"
      to: "@salesforce/sf-plugins-core"
      via: "import { SfCommand, Flags } from '@salesforce/sf-plugins-core'"
      pattern: "SfCommand"
---

<objective>
Create all 5 placeholder command files under src/commands/log/ and their corresponding
messages/ help text files, establishing the complete `sf log` command namespace.

Purpose: oclif discovers commands from the lib/commands directory structure. All 5 command
stubs must exist and compile so Phase 1-4 can implement each without changing the plugin
structure. Per D-01 (command namespace: sf log) and D-02 (scaffold all 5 commands now).

Output:
- 5 TypeScript command files in src/commands/log/
- 5 messages Markdown files in messages/
- All commands visible via `sf log --help` after compile + link
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/PROJECT.md
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/00-plugin-setup/00-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/00-plugin-setup/00-RESEARCH.md
@/Users/mc/Repos/sf-local-logs/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create messages/ directory with help text for all 5 commands</name>
  <files>
    messages/log.search.md,
    messages/log.trace.md,
    messages/log.download.md,
    messages/log.purge.md,
    messages/log.filter.md
  </files>
  <read_first>
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Pattern 4: Messages Markdown Files example
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Pitfall 4: Not Exporting Messages (explains file format)
    - .planning/ROADMAP.md — Phase details for each command's purpose
    - .planning/REQUIREMENTS.md — USER-01, DEBUG-01, DOWNLOAD-01, PURGE-01, FILTER-01 descriptions
  </read_first>
  <action>
    Create the messages/ directory and 5 Markdown files. oclif loads these via Messages.loadMessages().
    Format: H1 heading = message key, body = message text.

    messages/log.search.md:
    # summary
    Search for Salesforce users by name.

    # description
    Finds users in the target org by first name, last name, or both. Returns paginated results
    with user details. Implementation coming in Phase 1.

    # examples
    - <%= config.bin %> <%= command.id %> --query "John Doe" --target-org my-org
    - <%= config.bin %> <%= command.id %> --query "Smith" --target-org my-org --json

    messages/log.trace.md:
    # summary
    Initiate a debug log session for a Salesforce user.

    # description
    Creates a TraceFlag for the specified user so Salesforce generates debug logs for their
    activity. Implementation coming in Phase 2.

    # examples
    - <%= config.bin %> <%= command.id %> --user-id 005XXXXXXXXXXXXXXX --target-org my-org

    messages/log.download.md:
    # summary
    Download Salesforce debug logs to local disk.

    # description
    Downloads ApexLog records from the target org. Uses streaming I/O for large files.
    Respects the org's 1GB storage quota. Implementation coming in Phase 3.

    # examples
    - <%= config.bin %> <%= command.id %> --target-org my-org

    messages/log.purge.md:
    # summary
    Delete Salesforce debug logs to free storage quota.

    # description
    Interactively selects and deletes ApexLog records. Displays storage freed after deletion.
    Requires confirmation before deleting. Implementation coming in Phase 3.

    # examples
    - <%= config.bin %> <%= command.id %> --target-org my-org

    messages/log.filter.md:
    # summary
    Filter downloaded debug logs by keyword.

    # description
    Searches downloaded log files for a keyword (SObject name or Platform Event name).
    Displays matching lines with surrounding context. Supports export to file.
    Implementation coming in Phase 4.

    # examples
    - <%= config.bin %> <%= command.id %> --keyword "Account"
    - <%= config.bin %> <%= command.id %> --keyword "OrderEvent__e" --export results.txt

    Each file uses oclif's template syntax (<%= config.bin %> and <%= command.id %>) so
    the plugin name and command name are substituted at help generation time.
  </action>
  <verify>
    <automated>ls messages/log.search.md messages/log.trace.md messages/log.download.md messages/log.purge.md messages/log.filter.md</automated>
  </verify>
  <done>
    All 5 messages files exist in messages/. Each file contains at minimum a "# summary" section.
    grep -l "# summary" messages/log.*.md returns 5 files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 5 placeholder command files in src/commands/log/</name>
  <files>
    src/commands/log/search.ts,
    src/commands/log/trace.ts,
    src/commands/log/download.ts,
    src/commands/log/purge.ts,
    src/commands/log/filter.ts
  </files>
  <read_first>
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Pattern 1: Command Class with Org Flag (full example)
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Anti-patterns to Avoid section
    - .planning/phases/00-plugin-setup/00-RESEARCH.md — Example 2: Load Help Text from Messages
    - .planning/phases/00-plugin-setup/00-CONTEXT.md — Implementation Decisions: Initial Scaffolding section
    - messages/log.search.md (just created) — confirm message keys available
  </read_first>
  <behavior>
    - search command: exports default class Search extends SfCommand, has --target-org flag, --name flag (string), run() logs "User search coming in Phase 1" and returns { results: [] }
    - trace command: exports default class Trace extends SfCommand, has --target-org flag, --user-id flag (string), run() logs "Debug tracing coming in Phase 2" and returns { traceFlag: null }
    - download command: exports default class Download extends SfCommand, has --target-org flag, run() logs "Log download coming in Phase 3" and returns { logs: [] }
    - purge command: exports default class Purge extends SfCommand, has --target-org flag, run() logs "Log purge coming in Phase 3" and returns { deleted: 0, freedBytes: 0 }
    - filter command: exports default class Filter extends SfCommand, does NOT have --target-org flag (filter is a local operation on downloaded files), has --keyword flag (string), run() logs "Log filtering coming in Phase 4" and returns { matches: [] }
  </behavior>
  <action>
    Create all 5 command files. Each must follow the exact SfCommand pattern from RESEARCH.md.

    Template for org-dependent commands (search, trace, download, purge):

    import { SfCommand, Flags } from '@salesforce/sf-plugins-core';
    import { Messages } from '@salesforce/core';

    Messages.importMessagesDirectory(__dirname);
    const messages = Messages.loadMessages('sf-local-logs', 'log.COMMANDNAME');

    export type NAMEResult = { ... };

    export default class NAME extends SfCommand<NAMEResult> {
      public static readonly summary = messages.getMessage('summary');
      public static readonly description = messages.getMessage('description');
      public static readonly examples = messages.getMessages('examples');

      public static readonly flags = {
        'target-org': Flags.requiredOrgFlag(),
        'api-version': Flags.orgApiVersion(),
      };

      public async run(): Promise<NAMEResult> {
        const { flags } = await this.parse(NAME);
        this.log('PLACEHOLDER MESSAGE coming in Phase N');
        return { ... };
      }
    }

    Command-specific flag additions (do NOT implement logic, just declare flags per ROADMAP.md):

    search.ts:
    - Add flag: name: Flags.string({ char: 'n', summary: 'User name to search for', required: false })
    - Return type: SearchResult = { results: Array<{ id: string; name: string; email: string; lastLogin: string }> }
    - Placeholder return: { results: [] }

    trace.ts:
    - Add flag: 'user-id': Flags.string({ summary: 'Salesforce user ID to trace', required: false })
    - Return type: TraceResult = { traceFlag: { id: string; expirationDate: string; debugLevel: string } | null }
    - Placeholder return: { traceFlag: null }

    download.ts:
    - Return type: DownloadResult = { logs: Array<{ id: string; size: number; location: string }> }
    - Placeholder return: { logs: [] }

    purge.ts:
    - Return type: PurgeResult = { deleted: number; freedBytes: number }
    - Placeholder return: { deleted: 0, freedBytes: 0 }

    filter.ts (no org connection — operates on local files):
    - Remove 'target-org' and 'api-version' flags
    - Add flags: keyword: Flags.string({ char: 'k', summary: 'Keyword to filter for', required: false }),
                 export: Flags.string({ char: 'e', summary: 'File path to export results', required: false })
    - Return type: FilterResult = { matches: Array<{ file: string; line: number; content: string }> }
    - Placeholder return: { matches: [] }
    - Messages.loadMessages uses 'log.filter' key

    CRITICAL: Use 'sf-local-logs' (the package name from package.json) as the first argument
    to Messages.loadMessages(), not 'sf-cli-log-plugin'. Use Messages.importMessagesDirectory(__dirname)
    before loadMessages in each file.

    CRITICAL: Never extend SfdxCommand — only SfCommand from '@salesforce/sf-plugins-core'.

    After creating all 5 files, run: npm run compile
    Fix any TypeScript errors before marking this task done.
  </action>
  <verify>
    <automated>npm run compile 2>&1 | tail -5 && echo "Compile OK"</automated>
  </verify>
  <done>
    All 5 .ts files exist in src/commands/log/.
    npm run compile exits 0 with no TypeScript errors.
    Each file imports from '@salesforce/sf-plugins-core' (not '@salesforce/sfdx-core' or other legacy packages).
    grep -r "SfdxCommand" src/ returns empty (no legacy command class used).
    grep -r "SfCommand" src/commands/log/ returns 5 matches (one per file).
    After relink: sf log --help lists all 5 subcommands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| messages/ → command output | Help text loaded from Markdown files; no user-controlled input at this stage |
| command flags → org connection | Flags declare what user can pass; org auth handled by @salesforce/core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | Spoofing | SfCommand class inheritance | mitigate | Per RESEARCH.md anti-pattern: use SfCommand only. eslint-plugin-sf-plugin catches SfdxCommand usage at lint time. |
| T-00-05 | Information Disclosure | messages/ help text | accept | Help text is public documentation; no secrets or org-specific data in messages files. |
| T-00-06 | Tampering | Messages.loadMessages path | accept | messages/ path is static; no user input used to construct path at scaffolding stage. |
| T-00-SC | Tampering | npm/pip/cargo installs | mitigate | Packages already installed in Plan 01 from Approved audit table; no new installs in this plan. |
</threat_model>

<verification>
After both tasks complete:
1. `ls src/commands/log/*.ts | wc -l` — prints 5
2. `ls messages/log.*.md | wc -l` — prints 5
3. `npm run compile` — exits 0, no errors
4. `grep -r "SfdxCommand" src/` — empty output (no legacy class)
5. `grep -rn "SfCommand" src/commands/log/` — 5 matches
6. `sf plugin link . --no-verify && sf log --help` — shows 5 subcommands listed
</verification>

<success_criteria>
- All 5 command stubs in src/commands/log/ compile without errors (INSTALL-01, INSTALL-02 per D-02)
- Each org-dependent command has Flags.requiredOrgFlag() (UX-05 per D-01 command namespace)
- filter command does NOT have requiredOrgFlag (local file operation — no org needed)
- All 5 messages files exist with # summary section (UX-04 — --help output complete)
- sf log --help shows all 5 subcommands after compile + link
</success_criteria>

<output>
Create `.planning/phases/00-plugin-setup/00-02-SUMMARY.md` when done
</output>
