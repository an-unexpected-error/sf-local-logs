---
phase: "04-log-filtering"
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/commands/log/filter.ts
  - messages/log.filter.md
autonomous: true
requirements:
  - FILTER-01
  - UX-01
  - UX-02
must_haves:
  truths:
    - "src/commands/log/filter.ts exists (not deleted) so the NUT test sf log filter --help still passes"
    - "Filter.summary still contains text mentioning keyword or filtering (so NUT help output check passes)"
    - "Filter.flags no longer has an 'export' flag (D-08: export flag removed)"
    - "Filter.flags no longer has a 'keyword' flag (keyword moved to trace.ts per D-01)"
    - "Filter.run() body does not say 'Log filtering coming in Phase 4' — updated to redirect users to sf log trace --keyword"
    - "messages/log.filter.md summary text still present (for NUT compat); description updated to reflect Phase 4 decision"
    - "messages/log.filter.md examples removed or updated to not show --export flag"
    - "npm test exits 0 with updated filter.test.ts assertions passing"
    - "filter.nut.ts sf log filter --help still passes"
  artifacts:
    - path: "src/commands/log/filter.ts"
      provides: "Hollowed-out filter command stub (NUT-compatible)"
      contains: "sf log trace --keyword"
    - path: "messages/log.filter.md"
      provides: "Updated filter messages without export flag"
  key_links:
    - from: "test/commands/log/filter.nut.ts"
      to: "src/commands/log/filter.ts"
      via: "sf log filter --help CLI invocation"
      pattern: "sf log filter"
---

<objective>
Hollow out the sf log filter command stub and update its messages to reflect the Phase 4 decision (D-01: filtering moved to sf log trace --keyword). The file must be preserved — not deleted — to avoid breaking the existing NUT test that checks sf log filter --help.

Purpose: D-01 drops the standalone filter command. D-08 removes the --export flag. The stub must be updated to guide users to the correct command (sf log trace --keyword) while staying NUT-compatible.

Output: filter.ts with hollowed run() body and no export/keyword flags; messages/log.filter.md updated without --export example.
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hollow out filter.ts — remove flags, update run() body (D-01, D-08)</name>
  <files>src/commands/log/filter.ts</files>
  <read_first>
    - src/commands/log/filter.ts — read the current full file; understand FilterResult type, flags (keyword, export), run() stub body
    - test/commands/log/filter.test.ts — the updated test from Plan 01; check what assertions must pass after hollowing out (no export flag, summary redirects to sf log trace)
    - test/commands/log/filter.nut.ts — confirm what the NUT tests check (specifically the help output text from Filter.summary); ensure nothing breaks
    - .planning/phases/04-log-filtering/04-RESEARCH.md — Assumption A2 (NUT checks 'Filter downloaded debug logs' text from messages/log.filter.md # summary line); Open Question 2 (recommendation: redirect message in run())
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-01 (command dropped; stub kept), D-08 (export flag removed)
    - messages/log.filter.md — the current messages file; summary line must be preserved for NUT compat
  </read_first>
  <action>
    Rewrite src/commands/log/filter.ts in place. The new version must:

    1. Keep the same file structure: ESM imports (fileURLToPath, dirname, SfCommand, Flags, Messages), Messages.importMessagesDirectory, Messages.loadMessages('sf-local-logs', 'log.filter').

    2. Remove the old FilterResult type (the one with matches: Array<{file, line, content}>). The FilterResult that matters for the plugin is now in src/types/download.ts (Plan 02). This file can return void or a minimal empty type.

    3. Remove all flags from Filter.flags:
       - Remove the keyword flag (keyword functionality lives in trace.ts per D-01)
       - Remove the export flag (per D-08)
       - Filter.flags should be an empty object {} or only retain target-org if needed for help text

    4. Change the run() body to output a guidance message instead of the old stub text:
       this.log('Use `sf log trace --keyword <keyword>` to filter downloaded logs. The standalone filter command is not available in v1.');
       return {};

    5. The class return type should be changed to FilterCommandResult (a minimal local type: `{}` or `Record<string, never>`) since FilterResult is now in download.ts and has a different shape.

    6. The class definition line: export default class Filter extends SfCommand<Record<string, never>>

    CRITICAL: Do NOT remove or rename the class. Do NOT delete the file. The filter.nut.ts NUT test invokes sf log filter --help which relies on the class existing and having summary text from messages/log.filter.md.

    After rewriting, verify that Filter.summary still loads (it comes from messages/log.filter.md # summary which is NOT being changed in this file). The filter.test.ts 'has summary text' assertion must still pass.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test 2>&1 | grep -E "(log filter|filter command|export flag|passing|failing)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/log/filter.ts exports default class Filter (file not deleted)
    - Filter.flags does NOT have a property named 'export' (undefined per D-08)
    - Filter.flags does NOT have a property named 'keyword' (keyword lives in trace.ts per D-01)
    - Filter.run() body contains 'sf log trace --keyword' string (guidance message)
    - Filter.summary is still a non-empty string (loads from messages/log.filter.md # summary key)
    - npm run build exits 0
    - npm test exits 0 with filter.test.ts 'does not have export flag' test now passing
    - npm test exits 0 with filter.test.ts 'has summary text' test still passing
  </acceptance_criteria>
  <done>filter.ts hollowed out: no export/keyword flags, run() redirects users to trace --keyword, class still exported for NUT compat; npm test exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Update messages/log.filter.md to reflect Phase 4 decision (D-01, D-08, UX-01)</name>
  <files>messages/log.filter.md</files>
  <read_first>
    - messages/log.filter.md — current content: summary, description, examples with --export; must understand what NUT test checks from this file before changing it
    - test/commands/log/filter.nut.ts — verify exactly what text the NUT checks from help output; if it checks for 'Filter downloaded debug logs', the summary line must be preserved verbatim
    - .planning/phases/04-log-filtering/04-RESEARCH.md — Assumption A2: NUT checks 'Filter downloaded debug logs' text from # summary key
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-01 (stub kept; users guided to trace --keyword), D-08 (export removed)
  </read_first>
  <action>
    Rewrite messages/log.filter.md with these exact changes:

    1. Keep the # summary key and its text EXACTLY as-is: "Filter downloaded debug logs by keyword." — this text is checked by filter.nut.ts per Assumption A2. Do not change it.

    2. Replace the # description key body with text that reflects the Phase 4 decision:
       "Filtering is now integrated into the trace workflow. Use `sf log trace --keyword <keyword>` to filter downloaded logs by keyword. Matching logs remain in the session directory; non-matching logs move to rejected/."

    3. Replace the # examples block with a single example that shows the correct command:
       "<%= config.bin %> log trace --keyword \"Account\" --target-org my-org"

    Do NOT change the # summary line. The NUT test depends on it.
    Do NOT add new message keys to this file (the 4 filter-related keys were added to log.trace.md in Plan 02, not here).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && npm test 2>&1 | grep -E "(filter|passing|failing)" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - messages/log.filter.md contains `# summary` key with text "Filter downloaded debug logs by keyword."
    - messages/log.filter.md # description does NOT mention --export or "Implementation coming in Phase 4"
    - messages/log.filter.md # description contains 'sf log trace --keyword'
    - messages/log.filter.md # examples does NOT show --export flag
    - npm run build exits 0
    - npm test exits 0 (filter.test.ts 'has summary text' still passes)
  </acceptance_criteria>
  <done>messages/log.filter.md updated: summary preserved for NUT compat, description redirects to trace --keyword, --export example removed; npm test exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → sf log filter | User invokes deprecated command; output is a guidance string only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | filter.ts run() output | accept | Guidance message reveals only command usage; no org data or credentials exposed |
| T-04-04-02 | Spoofing | NUT test compatibility | accept | Filter class preserved in same file with same summary; NUT will continue to pass without spoofing risk |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install step in this plan |
</threat_model>

<verification>
After both tasks complete:

```
npm run build
```
Must exit 0. filter.ts rewrite must compile cleanly.

```
npm test
```
Must exit 0. filter.test.ts must pass all 4 assertions including 'does not have export flag'. Pre-existing 261 tests must still pass.

NUT verification (requires sf CLI linked):
```
sf log filter --help
```
Expected: Help output includes "Filter downloaded debug logs by keyword." (from # summary). Does not mention --export flag.
</verification>

<success_criteria>
- filter.ts exists with no keyword or export flags; run() body redirects to trace --keyword
- messages/log.filter.md summary unchanged for NUT compatibility; description and examples updated
- filter.test.ts all 4 assertions pass: class defined, has summary text, no export flag, summary mentions trace
- npm run build exits 0; npm test exits 0
</success_criteria>

<output>
Create .planning/phases/04-log-filtering/04-04-SUMMARY.md when done
</output>
