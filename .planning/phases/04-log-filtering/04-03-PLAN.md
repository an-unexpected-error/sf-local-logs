---
phase: "04-log-filtering"
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/commands/log/trace.ts
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
  - UX-01
  - UX-02
  - UX-04
must_haves:
  truths:
    - "sf log trace --keyword Account filters downloaded logs by 'Account' after download completes"
    - "Trace.flags['keyword'] is declared as an optional string flag with char 'k'"
    - "TraceWithDownloadResult type includes optional filterResult field of type FilterResult"
    - "After filtering, a summary message is logged: 'X log(s) matched \"keyword\", Y moved to rejected/. Logs saved to path'"
    - "If --keyword is not provided, trace.ts behavior is identical to Phase 3 (no filtering step)"
    - "If sessionDir is undefined (no downloads), filtering is skipped with a warning"
    - "If downloadResults is empty, filtering is skipped with filterSkippedNoDownloads message"
    - "filterResult is attached to the return value for --json output (UX-04)"
    - "npm test exits 0 with keyword flag tests from Plan 01 now passing"
  artifacts:
    - path: "src/commands/log/trace.ts"
      provides: "keyword flag + filterDownloadedLogs integration"
      contains: "keyword.*Flags.string"
  key_links:
    - from: "src/commands/log/trace.ts"
      to: "src/utils/filter-helper.ts"
      via: "import filterDownloadedLogs"
      pattern: "filterDownloadedLogs.*filter-helper"
    - from: "src/commands/log/trace.ts"
      to: "src/types/download.ts"
      via: "FilterResult type import"
      pattern: "FilterResult.*download"
---

<objective>
Wire the --keyword flag and filtering logic into src/commands/log/trace.ts. This plan integrates the filter-helper.ts utility (Plan 02) with the existing trace command flow, extending the return type and adding the post-download filter step.

Purpose: FILTER-01 requires the user to invoke filtering via sf log trace --keyword; FILTER-02 requires a summary output; UX-04 requires filterResult in --json output. All three are satisfied by extending trace.ts.

Output: trace.ts with keyword flag, filterDownloadedLogs call, summary log output, extended TraceWithDownloadResult type.
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend trace.ts with --keyword flag, filterDownloadedLogs call, and extended return type (FILTER-01, FILTER-02, UX-01, UX-02, UX-04, D-01 through D-10)</name>
  <files>src/commands/log/trace.ts</files>
  <read_first>
    - src/commands/log/trace.ts — read the entire file; understand every flag declaration, the run() method structure, download flow (Steps 8 onward), TraceWithDownloadResult type, return statement
    - src/utils/filter-helper.ts — exported function signatures (from Plan 02): scanFirstNLines and filterDownloadedLogs
    - src/types/download.ts — FilterResult interface (from Plan 02); import it for the return type extension
    - messages/log.trace.md — the 4 new message keys added in Plan 02: flagKeyword, filterSummary, filterSkippedNoDownloads, filterError
    - .planning/phases/04-log-filtering/04-RESEARCH.md — Pattern 4 (flag declaration), Pattern 5 (summary log), Code Examples section (full integration pattern), Pitfall 4 (empty downloads guard), Pitfall 5 (sessionDir undefined guard)
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-01 (keyword flag not a separate command), D-02 (most recent session), D-09 (summary format), D-10 (no per-file status)
    - test/commands/log/trace.test.ts — keyword flag tests added in Plan 01 (these must pass after this plan)
  </read_first>
  <behavior>
    - Trace.flags['keyword'] is declared using Flags.string({ char: 'k', summary: messages.getMessage('flagKeyword'), required: false })
    - TraceWithDownloadResult type gains: filterResult?: FilterResult
    - In run(), after the download step and sessionDir assignment, a new Step 9 runs:
      - Extract: const keyword = flags['keyword']
      - Guard: if (keyword && sessionDir && downloadResults.length > 0): call filterDownloadedLogs(sessionDir, keyword), log summary via filterSummary message, attach filterResult to result
      - Guard: if (keyword && downloadResults.length === 0): log filterSkippedNoDownloads message
      - Guard: if (keyword && !sessionDir): log filterSkippedNoDownloads message
      - Error handling: wrap filterDownloadedLogs call in try/catch; on error: this.warn(messages.getMessage('filterError', [errorMsg, sessionDir]))
    - filterResult is assigned to result.filterResult before return when filtering ran
    - When keyword is not provided, run() behavior is byte-for-byte identical to Phase 3 (no new code paths execute)
  </behavior>
  <action>
    Modify src/commands/log/trace.ts with the following targeted changes. Read the file first and understand exact line numbers before editing.

    Change 1 — Add import at top of file (after existing imports):
    Import filterDownloadedLogs from '../../utils/filter-helper.js' and FilterResult from '../../types/download.js'. Use named imports.

    Change 2 — Extend TraceWithDownloadResult type:
    Add optional field `filterResult?: FilterResult` to the existing TraceWithDownloadResult type (the inline type defined near the top of the file). Do not create a new type; extend the existing one.

    Change 3 — Add keyword flag to Trace.flags:
    Inside the static flags object (currently has target-org, api-version, user-id, level, no-watch, overwrite), add:
      'keyword': Flags.string({
        char: 'k',
        summary: messages.getMessage('flagKeyword'),
        required: false,
      })

    Change 4 — Extract keyword in run():
    After const { flags } = await this.parse(Trace), add:
      const keyword = flags['keyword'];

    Change 5 — Insert Step 9 after download completes:
    In run(), after the existing download try/catch block (the block that sets sessionDir and logs downloadCompleted), insert a new comment "// Step 9: Filter downloaded logs if --keyword is provided" followed by the filter logic:

      if (keyword) {
        if (!sessionDir || downloadResults.length === 0) {
          this.log(messages.getMessage('filterSkippedNoDownloads'));
        } else {
          try {
            const filterResult = await filterDownloadedLogs(sessionDir, keyword);
            this.log(messages.getMessage('filterSummary', [
              filterResult.matched,
              keyword,
              filterResult.rejected,
              filterResult.sessionDir,
            ]));
            result.filterResult = filterResult;
          } catch (error) {
            const errorMsg = error instanceof Error ? error.message : String(error);
            this.warn(messages.getMessage('filterError', [errorMsg, sessionDir]));
          }
        }
      }

    The Step 9 block must come AFTER the download try/catch (which sets sessionDir and downloadResults) and BEFORE the table display and watch mode logic.

    Do not move or restructure any existing code. Insert Step 9 as a new block.

    After the changes, also add a keyword example to the messages/log.trace.md examples section if space allows — but ONLY if it doesn't require touching any other files. The messages file is already modified in Plan 02; this is an optional enhancement in this plan only if the executor judges it worth the minimal edit.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test 2>&1 | grep -E "(keyword|filterResult|passing|failing)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/log/trace.ts contains `'keyword': Flags.string({` with `char: 'k'` and `required: false`
    - src/commands/log/trace.ts contains `filterResult?: FilterResult` in TraceWithDownloadResult type
    - src/commands/log/trace.ts contains `import.*filterDownloadedLogs.*filter-helper`
    - src/commands/log/trace.ts contains `import.*FilterResult.*download`
    - src/commands/log/trace.ts contains `filterDownloadedLogs(sessionDir, keyword)` call in run()
    - src/commands/log/trace.ts contains guard: `if (keyword && (!sessionDir || downloadResults.length === 0))`  (or equivalent two-branch guard structure)
    - npm run build exits 0
    - npm test exits 0 with keyword flag tests from Plan 01 now passing (not skipped)
    - Trace.flags['keyword'].required === false (verified by trace.test.ts keyword flag tests)
  </acceptance_criteria>
  <done>trace.ts extended with keyword flag, filterDownloadedLogs integration, extended TraceWithDownloadResult; npm test exits 0 with all keyword tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user → keyword flag | --keyword value is user-supplied string; flows to filterDownloadedLogs |
| filterDownloadedLogs → filesystem | operates on internally-constructed sessionDir only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | keyword flag value → includes() call | accept | keyword flows to filterDownloadedLogs which uses String.prototype.includes() only; no path construction uses keyword; no eval/regex injection risk |
| T-04-03-02 | DoS | keyword flag causing full-file scan | mitigate | scanFirstNLines hard-limits to 100 lines per D-03; readline streams line-by-line without buffering |
| T-04-03-03 | Information Disclosure | filterError message exposes sessionDir | accept | sessionDir is an internal plugin-managed path (no secrets); disclosing it helps the user remediate the error |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; filter-helper.ts and download.ts are local imports |
</threat_model>

<verification>
After task completes:

```
npm run build
```
Must exit 0. New imports of filter-helper.js and FilterResult from download.js must resolve.

```
npm test
```
Must exit 0. All keyword flag tests from Plan 01 trace.test.ts now pass. Pre-existing 261 tests still pass.

Manual verification (after org authentication):
```
sf log trace --keyword Account --target-org <my-org> --no-watch
```
Expected: trace created, logs downloaded, summary line printed: "N log(s) matched "Account", M moved to rejected/. Logs saved to ~/.local/share/sf/plugin-logs/..."
</verification>

<success_criteria>
- trace.ts has keyword flag declared with char 'k', required: false, summary from messages
- TraceWithDownloadResult.filterResult?: FilterResult added to type
- filterDownloadedLogs called after download; result attached to return value
- Guards for empty downloads and undefined sessionDir prevent nil reference crashes
- Error in filterDownloadedLogs warns but does not fail the entire command
- npm run build exits 0; npm test exits 0
</success_criteria>

<output>
Create .planning/phases/04-log-filtering/04-03-SUMMARY.md when done
</output>
