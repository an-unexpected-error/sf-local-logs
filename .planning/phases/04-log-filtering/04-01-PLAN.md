---
phase: "04-log-filtering"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/utils/filter-helper.test.ts
  - test/commands/log/trace.test.ts
  - test/commands/log/filter.test.ts
autonomous: true
requirements:
  - FILTER-01
  - FILTER-02
  - FILTER-03
  - UX-01
  - UX-02
  - UX-04
must_haves:
  truths:
    - "test/utils/filter-helper.test.ts exists and imports from src/utils/filter-helper.js"
    - "filter-helper tests include failing stubs for scanFirstNLines and filterDownloadedLogs covering FILTER-01, FILTER-03"
    - "trace.test.ts keyword flag tests are added to the Flag Configuration describe block"
    - "filter.test.ts is updated to assert the hollowed-out Filter class state (no run stub, class still exported)"
    - "npm test exits 0 with all pre-existing 261 tests still passing (new stubs may fail or be pending)"
  artifacts:
    - path: "test/utils/filter-helper.test.ts"
      provides: "Unit test stubs for filter-helper utility"
      contains: "describe('scanFirstNLines'"
    - path: "test/commands/log/trace.test.ts"
      provides: "keyword flag assertions in Flag Configuration block"
      contains: "keyword flag"
    - path: "test/commands/log/filter.test.ts"
      provides: "Updated filter command assertions for hollowed-out state"
  key_links:
    - from: "test/utils/filter-helper.test.ts"
      to: "src/utils/filter-helper.js"
      via: "ESM import"
      pattern: "from '../../src/utils/filter-helper.js'"
---

<objective>
Create test stubs for Phase 4 filtering functionality. This is the Wave 0 (Nyquist) plan that ensures every subsequent implementation task has a test waiting for it before code is written.

Purpose: Establish the test contracts that define what scanFirstNLines(), filterDownloadedLogs(), the --keyword flag, and the hollowed-out filter command must do. Implementation plans run after this plan to make these tests pass.

Output: Three test files with behavior-asserting stubs ready for implementations in Wave 2+ plans.
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-RESEARCH.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/04-log-filtering/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create filter-helper test stubs (FILTER-01, FILTER-02, FILTER-03)</name>
  <files>test/utils/filter-helper.test.ts</files>
  <read_first>
    - test/utils/storage-manager.test.ts — existing utils test file for import patterns, describe/it style, mocha/chai usage
    - test/utils/download-helper.test.ts — second example of utils test style and sinon usage
    - .planning/phases/04-log-filtering/04-RESEARCH.md — Pattern 1 (scanFirstNLines signature), Pattern 2 (filterDownloadedLogs signature), Validation Architecture section for exact behaviors to cover
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-03 (100 lines), D-04 (case-insensitive), D-07 (non-matching moves to rejected/)
  </read_first>
  <behavior>
    - scanFirstNLines(filePath, 100, keyword) returns true when keyword appears in first 100 lines (case-insensitive)
    - scanFirstNLines returns false when keyword is absent from first 100 lines
    - scanFirstNLines returns false when keyword is present only after line 100
    - scanFirstNLines is case-insensitive: "account" matches "Account" and "ACCOUNT"
    - filterDownloadedLogs(sessionDir, keyword) returns { matched: number, rejected: number, sessionDir: string }
    - filterDownloadedLogs moves non-matching .json files to sessionDir/rejected/ (FILTER-03, D-07)
    - filterDownloadedLogs does NOT delete non-matching files — only renames them to rejected/
    - filterDownloadedLogs excludes metadata.json from scanning (not a log file)
  </behavior>
  <action>
    Create test/utils/filter-helper.test.ts with mocha/chai/sinon tests. Follow the import and describe structure from storage-manager.test.ts exactly (ESM imports, no top-level await, describe/it blocks).

    Import scanFirstNLines and filterDownloadedLogs from '../../src/utils/filter-helper.js'. These functions do not exist yet — the file will be created in Plan 02. The import will cause a compile error until Plan 02 creates the file; that is expected for Wave 0 stubs.

    Write test stubs using sinon to mock node:fs and node:readline so tests run without real files. Use sinon.stub() on the fs/promises module and on readline.createInterface. Structure the mocks so that:
    - For scanFirstNLines tests: stub createReadStream and createInterface to emit controlled lines
    - For filterDownloadedLogs tests: stub readdir to return a fixed file list, stub rename to capture calls, stub mkdir

    Key test cases to write (all in this file):

    describe('scanFirstNLines') block:
    - 'returns true when keyword found in first 100 lines (case-insensitive)'
    - 'returns false when keyword absent from first 100 lines'
    - 'is case-insensitive: lowercase keyword matches uppercase line'
    - 'returns false when keyword only appears after line 100'

    describe('filterDownloadedLogs') block:
    - 'returns { matched, rejected, sessionDir } object shape'
    - 'moves non-matching log files to rejected/ subfolder using rename'
    - 'does not move matching log files'
    - 'excludes metadata.json from scan and move operations'
    - 'creates rejected/ directory with mkdir recursive before moving files'
    - 'returns matched count equal to number of matching files'
    - 'returns rejected count equal to number of non-matching files'

    Important: tests that are difficult to stub in Wave 0 may be written as pending (using it.skip or wrapping expectations in try/catch) — but all describe blocks and it() names must exist. The critical constraint is that the import line exists and the function names are referenced, so that Plan 02's implementation can be verified by running npm test.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; echo "---"; npm test 2>&1 | grep -E "(passing|failing|filter-helper)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test/utils/filter-helper.test.ts exists
    - File contains `import.*scanFirstNLines.*from '../../src/utils/filter-helper.js'`
    - File contains `import.*filterDownloadedLogs.*from '../../src/utils/filter-helper.js'`
    - File contains describe blocks: 'scanFirstNLines' and 'filterDownloadedLogs'
    - File contains at least 7 distinct it() or it.skip() calls covering the behaviors listed
    - npm test exits 0 (existing 261 tests still pass; new tests may be pending/skipped until Plan 02)
  </acceptance_criteria>
  <done>test/utils/filter-helper.test.ts created, imports both functions, contains stubs for all 7 behaviors, npm test exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend trace.test.ts with keyword flag assertions (FILTER-01, UX-04)</name>
  <files>test/commands/log/trace.test.ts</files>
  <read_first>
    - test/commands/log/trace.test.ts — read the entire file to understand existing Flag Configuration and Flag Descriptions describe blocks; new tests must be added inside these existing blocks
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-05 (single string, required: false), CONTEXT code_context "Flag parsing" pattern
    - .planning/phases/04-log-filtering/04-RESEARCH.md — Pattern 4 (keyword flag declaration), UX-04 row in Phase Requirements table
  </read_first>
  <behavior>
    - Trace.flags['keyword'] exists after Plan 03 adds the flag
    - Trace.flags['keyword'].required is false (keyword is optional)
    - Trace.flags['keyword'] has a summary string (message key flagKeyword)
    - TraceWithDownloadResult type shape can include filterResult property (verified via JSON output structure assertion)
  </behavior>
  <action>
    Append new test cases to the existing describe blocks in test/commands/log/trace.test.ts. Do NOT rewrite the file. Add the following tests:

    Inside the existing describe('Flag Configuration') block (near line 52), add:
    - it('keyword flag is present') — asserts Trace.flags['keyword'] exists
    - it('keyword flag is optional string') — asserts Trace.flags['keyword'].required === false

    Inside the existing describe('Flag Descriptions') block (near line 84), add:
    - it('keyword flag has summary description') — asserts Trace.flags['keyword'] has a summary property that is a non-empty string

    Add a new top-level describe block 'Filter Integration (keyword flag)' after the existing blocks:
    - it('keyword flag char is k') — asserts (Trace.flags['keyword'] as { char?: string }).char === 'k'
    - it('filterResult is optional in TraceWithDownloadResult shape') — this is a TypeScript structural test; verify by importing the type from trace.ts and asserting no compile error when filterResult is omitted (use a type-level check or skip if not easily testable at runtime)

    These tests will fail until Plan 03 adds the keyword flag to trace.ts. That is expected behavior for Wave 0 stubs.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "(keyword|filterResult|passing|failing)" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - test/commands/log/trace.test.ts contains `it('keyword flag is present'`
    - test/commands/log/trace.test.ts contains `it('keyword flag is optional string'`
    - test/commands/log/trace.test.ts contains `it('keyword flag has summary description'`
    - Pre-existing 261 tests still pass (npm test exits 0; new keyword tests may fail until Plan 03)
  </acceptance_criteria>
  <done>Three new keyword flag tests added to trace.test.ts; existing tests unaffected; npm test exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Update filter.test.ts for hollowed-out Filter command state (D-01, D-08)</name>
  <files>test/commands/log/filter.test.ts</files>
  <read_first>
    - test/commands/log/filter.test.ts — read current contents (2 test assertions about Filter class and summary)
    - src/commands/log/filter.ts — current stub implementation; note that Filter.summary, Filter.flags.keyword, and Filter.flags.export exist
    - .planning/phases/04-log-filtering/04-CONTEXT.md — D-01 (filter command dropped; stub kept), D-08 (--export flag removed)
    - .planning/phases/04-log-filtering/04-RESEARCH.md — State of the Art section, Assumption A2 (NUT checks 'Filter downloaded debug logs' text in help)
  </read_first>
  <action>
    Replace the contents of test/commands/log/filter.test.ts. The updated file must:

    1. Keep the existing two assertions: 'command class is defined' and 'has summary text' — these must continue to pass after Plan 04 hollows out the stub.

    2. Add three new assertions that verify the post-Plan-04 state:
       - 'does not have export flag' — asserts Filter.flags['export'] is undefined (per D-08, export flag removed)
       - 'summary text mentions keyword or trace' — asserts Filter.summary contains at least one of 'keyword', 'trace', or 'sf log trace' (the hollowed-out summary should guide users to the right command)
       - 'FilterResult type from filter.ts has matches array' — this assertion checks the original FilterResult type still exports cleanly; it can remain as-is or be relaxed to just verify Filter is importable

    The first two original tests must pass immediately (Filter.summary still loads). The new 'does not have export flag' test will fail until Plan 04 removes the export flag. That is expected Wave 0 behavior.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "(filter|passing|failing)" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - test/commands/log/filter.test.ts contains 'command class is defined' test (unchanged)
    - test/commands/log/filter.test.ts contains 'has summary text' test (unchanged)
    - test/commands/log/filter.test.ts contains `it('does not have export flag'`
    - test/commands/log/filter.test.ts contains `it('summary text mentions` or similar summary guidance test
    - npm test exits 0 (existing 261 tests still pass; 'does not have export flag' fails until Plan 04)
  </acceptance_criteria>
  <done>filter.test.ts updated with 4 total tests; pre-existing class and summary tests still pass; npm test exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → src | Tests import production source; no untrusted external input in test files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | test/utils/filter-helper.test.ts sinon stubs | accept | Test-only stubs; no production code path; sinon sandboxes restore after each test |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed in this plan; all imports are existing mocha/chai/sinon |
</threat_model>

<verification>
Run full test suite after all three tasks complete:
```
npm test
```
Expected: All 261 pre-existing tests pass. New keyword flag tests in trace.test.ts and the export-flag test in filter.test.ts will fail (intentionally — they await implementations in Plans 02-04).

Run build to confirm TypeScript compiles (filter-helper.ts import will error until Plan 02; if ts compilation fails, add a compile-skip or use require() with try/catch in the test file):
```
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- test/utils/filter-helper.test.ts created with imports and 7+ behavior stubs for scanFirstNLines and filterDownloadedLogs
- test/commands/log/trace.test.ts has 3+ new keyword flag assertions added to existing describe blocks
- test/commands/log/filter.test.ts updated with 2 original + 2 new assertions for hollowed-out state
- npm test exits 0 with all 261 pre-existing tests passing
- Build TypeScript compiles without unresolvable errors that would break the existing test suite
</success_criteria>

<output>
Create .planning/phases/04-log-filtering/04-01-SUMMARY.md when done
</output>
