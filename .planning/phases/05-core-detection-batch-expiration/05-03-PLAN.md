---
phase: "05-core-detection-batch-expiration"
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/commands/log/trace.ts
  - test/commands/log/trace.test.ts
autonomous: true
requirements:
  - TRACE-01
  - TRACE-03
  - TRACE-08

must_haves:
  truths:
    - "trace.ts calls detectAndExpireOverlappingTraces() BEFORE createTraceFlag() in the run() method (per D-04, D-05)"
    - "trace.ts no longer calls checkExistingTraceFlag() for the overlap path — one detection call only (per RESEARCH.md Pitfall 1: double-query avoidance)"
    - "When stoppedTraces.length > 0, trace.ts logs statusDetectedOverlappingTraces then statusStoppingOverlappingTraces messages (per D-11)"
    - "stoppedTraces array is included in the returned TraceWithDownloadResult (per D-10)"
    - "Integration test verifies trace.ts wires detectAndExpireOverlappingTraces and populates result.stoppedTraces (per TRACE-01, TRACE-08)"
    - "If detectAndExpireOverlappingTraces throws (batch update failure), trace.ts surfaces the error and does NOT proceed to createTraceFlag() (per D-05, D-12)"
  artifacts:
    - path: "src/commands/log/trace.ts"
      provides: "Trace command with overlap detection + expiration integrated before trace creation"
      contains: "detectAndExpireOverlappingTraces"
    - path: "test/commands/log/trace.test.ts"
      provides: "Integration tests for overlap detection wiring in Trace command"
      contains: "stoppedTraces"
  key_links:
    - from: "src/commands/log/trace.ts"
      to: "src/utils/trace-helper.ts"
      via: "import { detectAndExpireOverlappingTraces } from '../../utils/trace-helper.js'"
      pattern: "detectAndExpireOverlappingTraces"
    - from: "src/commands/log/trace.ts"
      to: "messages/log.trace.md"
      via: "messages.getMessage('statusDetectedOverlappingTraces', [...])"
      pattern: "statusDetectedOverlappingTraces"
---

<objective>
Wire detectAndExpireOverlappingTraces() into the trace command and verify the full command flow handles overlap detection correctly end-to-end.

Purpose: Plans 01 and 02 created the contract and the function. This plan integrates them into the working command and proves the integration works through tests. After this plan, Phase 5 is complete — overlap detection, batch expiration, result extension, and CLI output are all wired together.

Output:
- Modified src/commands/log/trace.ts: replaces checkExistingTraceFlag() call with detectAndExpireOverlappingTraces(), logs overlap status messages, includes stoppedTraces in result
- Integration tests in test/commands/log/trace.test.ts covering the overlap wiring behavior
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-01-SUMMARY.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire detectAndExpireOverlappingTraces() into trace.ts and update imports</name>
  <files>src/commands/log/trace.ts</files>
  <read_first>
    - src/commands/log/trace.ts — read full file; focus on Step 2 (lines ~124-128, the checkExistingTraceFlag call), the import statement line 8, the result construction at Step 7 (lines ~177-186), and TraceWithDownloadResult type definition (lines ~39-43)
    - src/utils/trace-helper.ts — confirm detectAndExpireOverlappingTraces is exported (from Plan 02)
    - src/types/trace.ts — confirm stoppedTraces optional field exists on TraceResult (from Plan 01)
    - messages/log.trace.md — confirm statusDetectedOverlappingTraces, statusStoppingOverlappingTraces, statusStoppedTrace keys exist (from Plan 01)
    - .planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md — Pattern 3 (integration into trace command flow), Pitfall 1 (double-query avoidance)
    - .planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md — D-04 (check before create), D-05 (sequence), D-09 (display details + auto-expire), D-11 (log summary message)
  </read_first>
  <behavior>
    - Import detectAndExpireOverlappingTraces from trace-helper (add to existing import on line 8)
    - Remove checkExistingTraceFlag from import (it is no longer called in the overlap path; confirm nothing else in trace.ts calls it before removing)
    - At Step 2 (before createTraceFlag call): call detectAndExpireOverlappingTraces(org, userId) and store result in const stoppedTraces
    - If stoppedTraces.length > 0: log statusDetectedOverlappingTraces with stoppedTraces.length, then log statusStoppingOverlappingTraces with stoppedTraces.length (Phase 6 will add per-trace detail logging)
    - Remove the existing overwrite flag check block (lines ~126-128: if (overwrite) { this.log('statusStoppingExistingTrace') }) — overlap handling is now automatic; the overwrite flag's purpose shifts to Phase 8 confirmation skipping
    - At Step 7, include stoppedTraces in the result object: result.stoppedTraces = stoppedTraces (or include inline during construction)
    - If detectAndExpireOverlappingTraces throws (batch update failure), let the error propagate to the outer try-catch at line ~301, which re-throws with the error message — do NOT wrap it in another message
  </behavior>
  <action>
    Modify src/commands/log/trace.ts as follows:

    1. Update the import on line 8 from trace-helper.js: replace checkExistingTraceFlag with detectAndExpireOverlappingTraces in the named imports list. Keep all other imported names (getDefaultDebugLevel, createTraceFlag, getDebugLevelName, initiateDownloadAfterTrace, formatDownloadProgress).

    2. Remove the `overwrite` variable assignment from the flags destructuring (const overwrite = flags['overwrite']) since checkExistingTraceFlag is no longer called. Keep the overwrite flag definition in the static flags object (Phase 8 will use it for confirmation skipping).

    3. Replace Step 2 entirely (the checkExistingTraceFlag call and the if (overwrite) log block) with:
       - const stoppedTraces = await detectAndExpireOverlappingTraces(org, userId);
       - if (stoppedTraces.length > 0) { this.log(messages.getMessage('statusDetectedOverlappingTraces', [stoppedTraces.length])); this.log(messages.getMessage('statusStoppingOverlappingTraces', [stoppedTraces.length])); }

    4. In the result construction at Step 7, add stoppedTraces to the TraceWithDownloadResult object. The TraceResult base type already has optional stoppedTraces from Plan 01. Add: stoppedTraces, as a top-level field alongside traceFlag.

    5. Do not modify any other step, flag definition, or method. The overwrite flag remains in the static flags object for Phase 8 to use.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/commands/log/trace.ts imports detectAndExpireOverlappingTraces (grep returns a match: "import.*detectAndExpireOverlappingTraces.*from")
    - src/commands/log/trace.ts does NOT call checkExistingTraceFlag (grep for "checkExistingTraceFlag(" returns no matches in the function body)
    - src/commands/log/trace.ts contains "statusDetectedOverlappingTraces" as a messages.getMessage argument
    - src/commands/log/trace.ts contains "stoppedTraces" in the result construction block (the Step 7 section)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - The overwrite flag remains in Trace.flags static definition (grep "overwrite.*Flags.boolean" returns a match)
  </acceptance_criteria>
  <done>trace.ts calls detectAndExpireOverlappingTraces before createTraceFlag, logs overlap messages, and includes stoppedTraces in the returned result. TypeScript compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for overlap wiring in trace command</name>
  <files>test/commands/log/trace.test.ts</files>
  <read_first>
    - test/commands/log/trace.test.ts — read the full existing test file to understand sinon sandbox setup, import structure, and existing describe blocks; note which functions are already imported and tested
    - test/utils/trace-helper.test.ts — reference for how detectAndExpireOverlappingTraces is stubbed with sinon (from Plan 02)
    - src/commands/log/trace.ts — confirm updated integration points (stoppedTraces in result, detectAndExpireOverlappingTraces call) after Task 1
    - .planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md — Validation Architecture: integration test cases for trace command with overlapping traces
  </read_first>
  <behavior>
    - Integration test "detectAndExpireOverlappingTraces is exported from trace-helper": import it and assert it is a function
    - Integration test "stoppedTraces array shape is correct when traces stopped": given stoppedTraces = [{id: 'tf001', expirationDate: '2026-06-02T00:00:00Z'}], verify each element has id (string) and expirationDate (string) properties
    - Integration test "Trace.flags overwrite flag still exists": Trace.flags['overwrite'] is present and is a Flags.boolean definition (Phase 8 relies on it)
    - Integration test "detectAndExpireOverlappingTraces function signature": accepts (org, userId) — verify it is exported as an async function with the correct arity (function.length === 2)
  </behavior>
  <action>
    Add a new describe block 'Overlap Detection Integration (Phase 5)' to test/commands/log/trace.test.ts. Add the import for detectAndExpireOverlappingTraces from '../../../src/utils/trace-helper.js' to the existing import at the top of the file.

    The four integration tests to add:

    Test 1 "detectAndExpireOverlappingTraces is exported as an async function":
    import detectAndExpireOverlappingTraces; assert expect(detectAndExpireOverlappingTraces).to.be.a('function').

    Test 2 "detectAndExpireOverlappingTraces accepts 2 parameters":
    assert expect(detectAndExpireOverlappingTraces.length).to.equal(2).

    Test 3 "stoppedTraces element type matches TraceResult contract":
    const example = { id: 'tf001', expirationDate: '2026-06-02T00:00:00Z' };
    assert expect(example.id).to.be.a('string') and expect(example.expirationDate).to.be.a('string') and expect(Object.keys(example)).to.deep.equal(['id', 'expirationDate']).

    Test 4 "Trace.flags overwrite flag is still defined (for Phase 8)":
    access Trace.flags as Record<string, unknown>; assert expect(Trace.flags['overwrite']).to.exist.

    Do not use sandbox.stub() for these four tests — they are structural/contract tests that verify exports and types, not behavior tests.
  </action>
  <verify>
    <automated>npx mocha --require ts-node/register test/commands/log/trace.test.ts --grep "Overlap Detection Integration" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/commands/log/trace.test.ts contains describe block "Overlap Detection Integration (Phase 5)"
    - File imports detectAndExpireOverlappingTraces from '../../../src/utils/trace-helper.js'
    - All 4 tests in "Overlap Detection Integration" describe block pass when mocha is run with --grep flag
    - npm test exits 0 (full suite: no regressions in existing tests, all new tests pass)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Four integration tests pass. Full test suite exits 0. Phase 5 implementation is complete and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detectAndExpireOverlappingTraces() result → trace.ts | Returned stoppedTraces array is trusted (comes from Salesforce API via trace-helper); no additional validation needed in trace.ts |
| Batch update failure → CLI error message | Error message from trace-helper is passed through to CLI; message may include Salesforce API details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Batch update error propagation | accept | Salesforce API errors surfaced to CLI user are appropriate (they are the operator); no PII or secrets in TraceFlag API errors |
| T-05-06 | Elevation of Privilege | trace.ts removing overwrite check | accept | overwrite flag remains in Trace.flags for Phase 8; Phase 5 auto-expires without prompt (Phase 8 adds confirmation gate); no privilege escalation: Tooling API enforces Salesforce org permissions |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx tsc --noEmit exits 0 (all three modified files compile)
- npm test exits 0 (full suite including all existing and new tests)
- npx mocha --require ts-node/register test/utils/trace-helper.test.ts --grep "detectAndExpireOverlappingTraces" — all 5 unit tests pass
- npx mocha --require ts-node/register test/commands/log/trace.test.ts --grep "Overlap Detection Integration" — all 4 integration tests pass
- grep "detectAndExpireOverlappingTraces" src/commands/log/trace.ts returns a match
- grep "checkExistingTraceFlag(" src/commands/log/trace.ts returns no match (call site removed)
- grep "stoppedTraces" src/commands/log/trace.ts returns matches in both the call site and result construction
</verification>

<success_criteria>
- trace.ts calls detectAndExpireOverlappingTraces() before createTraceFlag() in all code paths
- Overlap detection result is logged (statusDetectedOverlappingTraces, statusStoppingOverlappingTraces)
- stoppedTraces is included in the returned TraceWithDownloadResult
- checkExistingTraceFlag() call removed from trace.ts (no double-query)
- 9 new tests total (5 unit + 4 integration) all pass GREEN
- Full test suite (npm test) exits 0
- Phase 5 requirements TRACE-01, TRACE-03, TRACE-08 all satisfied
</success_criteria>

<output>
Create `.planning/phases/05-core-detection-batch-expiration/05-03-SUMMARY.md` when done
</output>
