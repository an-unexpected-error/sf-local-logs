---
phase: "05-core-detection-batch-expiration"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/trace.ts
  - messages/log.trace.md
autonomous: true
requirements:
  - TRACE-01
  - TRACE-03
  - TRACE-08

must_haves:
  truths:
    - "TraceResult type accepts an optional stoppedTraces array with id and expirationDate per each stopped trace (per D-10)"
    - "Messages file contains the three new overlap status strings for use by trace.ts (per D-11)"
    - "TypeScript compiler accepts stoppedTraces as optional on TraceResult without requiring it on existing callsites"
  artifacts:
    - path: "src/types/trace.ts"
      provides: "Extended TraceResult type with optional stoppedTraces field"
      contains: "stoppedTraces"
    - path: "messages/log.trace.md"
      provides: "Overlap status messages for CLI output"
      contains: "statusStoppingOverlappingTraces"
  key_links:
    - from: "src/utils/trace-helper.ts"
      to: "src/types/trace.ts"
      via: "import { type TraceResult }"
      pattern: "stoppedTraces.*optional"
    - from: "src/commands/log/trace.ts"
      to: "messages/log.trace.md"
      via: "messages.getMessage('statusStoppingOverlappingTraces', ...)"
      pattern: "statusStoppingOverlappingTraces"
---

<objective>
Establish the type contract and message strings that all Phase 5 implementation plans build on.

Purpose: Plans 02 and 03 cannot compile or emit correct output until TraceResult includes stoppedTraces and the messages file has overlap status strings. Defining contracts before implementation prevents TypeScript errors downstream and avoids re-reading files mid-implementation.

Output:
- Extended TraceResult type (src/types/trace.ts) — adds optional stoppedTraces array per D-10
- New message keys in messages/log.trace.md — overlap status strings per D-11
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend TraceResult type with optional stoppedTraces field</name>
  <files>src/types/trace.ts</files>
  <read_first>
    - src/types/trace.ts — current TraceResult definition (read full file before editing)
    - .planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md — D-10: stoppedTraces shape is Array of {id: string; expirationDate: string}
  </read_first>
  <action>
    Add an optional stoppedTraces property to the TraceResult type in src/types/trace.ts. The property is optional (trailing ?) so existing callsites (trace.ts building result without stoppedTraces) continue to compile without changes.

    The stoppedTraces element shape (per D-10):
    - id: string — the Salesforce TraceFlag record ID that was expired
    - expirationDate: string — the ORIGINAL expiration date of the stopped trace (ISO 8601), before it was set to now

    Add a JSDoc comment on the new field explaining it is populated by Phase 5's detectAndExpireOverlappingTraces() and consumed by Phase 6 (display) and Phase 7 (--json output).

    Do NOT rename, remove, or restructure any existing field in TraceResult.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/trace.ts contains the string "stoppedTraces" as an optional property on TraceResult
    - src/types/trace.ts contains "id: string" and "expirationDate: string" inside the stoppedTraces element type
    - Running "npx tsc --noEmit" exits 0 (no TypeScript errors introduced)
    - Existing traceFlag property and all its sub-fields remain unchanged in the type definition
  </acceptance_criteria>
  <done>TraceResult type compiles cleanly and accepts stoppedTraces as optional; existing code that omits stoppedTraces still compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add overlap detection message strings to messages/log.trace.md</name>
  <files>messages/log.trace.md</files>
  <read_first>
    - messages/log.trace.md — full file; understand existing message key naming convention and format before appending
    - .planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md — D-11 exact message text: "Stopped {N} overlapping trace(s). Creating new trace..." and specifics section message strings
  </read_first>
  <action>
    Append three new message key sections to messages/log.trace.md, following the existing # key / body format used throughout that file.

    The three keys and their body text to add (per D-11 and CONTEXT.md specifics):

    Key: statusDetectedOverlappingTraces
    Body: Detected %d overlapping trace(s) for user. Stopping them before creating new trace...

    Key: statusStoppingOverlappingTraces
    Body: Stopped %d overlapping trace(s). Creating new trace...

    Key: statusStoppedTrace
    Body: Stopped trace %s (was expiring %s)

    These are the only new additions. Do not modify any existing key or body text.
  </action>
  <verify>
    <automated>grep -c "statusDetectedOverlappingTraces\|statusStoppingOverlappingTraces\|statusStoppedTrace" /Users/mc/Repos/sf-local-logs/messages/log.trace.md</automated>
  </verify>
  <acceptance_criteria>
    - messages/log.trace.md contains "# statusDetectedOverlappingTraces" as a heading
    - messages/log.trace.md contains "# statusStoppingOverlappingTraces" as a heading
    - messages/log.trace.md contains "# statusStoppedTrace" as a heading
    - All three bodies use %d or %s format specifiers matching their expected argument counts (statusDetectedOverlappingTraces: one %d; statusStoppingOverlappingTraces: one %d; statusStoppedTrace: one %s id + one %s date)
    - The grep command above returns 3
    - Existing message keys remain unchanged (no lines removed or modified)
  </acceptance_criteria>
  <done>Three new message keys are appended and syntactically consistent with existing entries in the messages file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type definition → implementation | Type contract shapes all downstream code; incorrect optional marking here causes silent runtime failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/types/trace.ts | mitigate | Make stoppedTraces optional (?) so existing non-overlap callsites compile without change; verify with npx tsc --noEmit |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all types are native TypeScript |
</threat_model>

<verification>
- npx tsc --noEmit exits 0 after both tasks complete
- grep -c "statusDetectedOverlappingTraces\|statusStoppingOverlappingTraces\|statusStoppedTrace" messages/log.trace.md returns 3
- grep "stoppedTraces" src/types/trace.ts returns a line with "?" (optional marker)
</verification>

<success_criteria>
- TraceResult type has optional stoppedTraces: Array<{id: string; expirationDate: string}> property
- messages/log.trace.md has statusDetectedOverlappingTraces, statusStoppingOverlappingTraces, statusStoppedTrace keys
- TypeScript compiles cleanly (npx tsc --noEmit exits 0)
- No existing code modified; only additive changes
</success_criteria>

<output>
Create `.planning/phases/05-core-detection-batch-expiration/05-01-SUMMARY.md` when done
</output>
