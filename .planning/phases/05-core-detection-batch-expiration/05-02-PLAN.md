---
phase: "05-core-detection-batch-expiration"
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/utils/trace-helper.ts
  - test/utils/trace-helper.test.ts
autonomous: true
requirements:
  - TRACE-01
  - TRACE-03
  - TRACE-08

must_haves:
  truths:
    - "detectAndExpireOverlappingTraces() queries TraceFlags WHERE TracedEntityId = userId AND ExpirationDate > now (per D-01, TRACE-01)"
    - "When active traces are found, all are expired in a single tooling.update() call with an array of {Id, ExpirationDate: now} objects (per D-07, TRACE-03)"
    - "When multiple traces exist (3+), all are expired in the same batch call (per D-02, TRACE-08)"
    - "When detection query fails, function logs warning and returns empty array — does not throw (per D-13)"
    - "When batch update fails, function throws with details of what succeeded (per D-12)"
    - "Function returns Array<{id: string; expirationDate: string}> matching the stoppedTraces element shape in TraceResult (per D-10)"
    - "Unit tests cover: no overlap, single trace, multiple traces (3+), query failure, batch update failure"
  artifacts:
    - path: "src/utils/trace-helper.ts"
      provides: "detectAndExpireOverlappingTraces() exported async function"
      exports: ["detectAndExpireOverlappingTraces"]
      contains: "detectAndExpireOverlappingTraces"
    - path: "test/utils/trace-helper.test.ts"
      provides: "Unit tests for detectAndExpireOverlappingTraces"
      contains: "detectAndExpireOverlappingTraces"
  key_links:
    - from: "src/utils/trace-helper.ts"
      to: "Salesforce Tooling API"
      via: "connection.tooling.query() then connection.tooling.update()"
      pattern: "tooling\\.update\\('TraceFlag',\\s*\\["
    - from: "test/utils/trace-helper.test.ts"
      to: "src/utils/trace-helper.ts"
      via: "import { detectAndExpireOverlappingTraces }"
      pattern: "detectAndExpireOverlappingTraces"
---

<objective>
Implement the core overlap detection and batch expiration function, with unit tests proving all behavior contracts.

Purpose: This is the primary Phase 5 deliverable. detectAndExpireOverlappingTraces() replaces the conceptual role of checkExistingTraceFlag() for the overlap handling path. It queries for all active (non-expired) TraceFlags for a user, expires them in a single batch Tooling API call, and returns an array of stopped trace details for downstream display and JSON output.

Output:
- detectAndExpireOverlappingTraces() exported from src/utils/trace-helper.ts
- Unit tests in test/utils/trace-helper.test.ts covering all five behavior contracts from RESEARCH.md Wave 0 gaps
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/.planning/ROADMAP.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md
@/Users/mc/Repos/sf-local-logs/.planning/phases/05-core-detection-batch-expiration/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detectAndExpireOverlappingTraces() unit tests (RED phase)</name>
  <files>test/utils/trace-helper.test.ts</files>
  <read_first>
    - test/utils/trace-helper.test.ts — does not yet contain detectAndExpireOverlappingTraces tests; read to understand existing test style (import paths, sinon sandbox usage, stub patterns for connection.tooling.query and tooling.update)
    - test/utils/download-helper.test.ts — reference for fake connection stub pattern (fakeConnection with tooling.query returning Promise.resolve)
    - .planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md — Validation Architecture section: five test cases required
    - src/types/trace.ts — confirm stoppedTraces element shape {id, expirationDate} is now present (from Plan 01)
  </read_first>
  <behavior>
    - Test case "no overlap": tooling.query returns empty records array → function returns []
    - Test case "single overlap": tooling.query returns [{Id: 'tf001', ExpirationDate: '2026-06-02T00:00:00Z'}], tooling.update returns [{id: 'tf001', success: true}] → function returns [{id: 'tf001', expirationDate: '2026-06-02T00:00:00Z'}]
    - Test case "multiple overlaps (3)": tooling.query returns 3 records → tooling.update called once with array of 3 objects → function returns array of 3 stopped traces
    - Test case "query fails": tooling.query rejects → function returns [] (does NOT throw)
    - Test case "batch update fails": tooling.query returns records, tooling.update rejects → function throws Error containing "Failed to expire overlapping traces"
  </behavior>
  <action>
    Add a new describe block 'detectAndExpireOverlappingTraces' to test/utils/trace-helper.test.ts. Add the import for detectAndExpireOverlappingTraces from '../../src/utils/trace-helper.js' to the existing import statement at the top of the file (or add a new import if the function does not exist yet — the tests should fail RED until Plan 02 Task 2 adds the implementation).

    Use sinon to create a fakeConnection stub in beforeEach. Stub both fakeConnection.tooling.query and fakeConnection.tooling.update as sinon.stub() functions. Use sandbox.restore() in afterEach.

    For the "single overlap" test: verify the return value is an array with one element where element.id === 'tf001' and element.expirationDate === '2026-06-02T00:00:00Z'.

    For the "multiple overlaps" test: verify tooling.update was called exactly once (sinon calledOnce assertion) and verify the argument was an array of length 3 (no individual sequential calls).

    For the "query fails" test: stub tooling.query to return Promise.reject(new Error('SOQL error')); assert the function resolves to [] and does NOT throw or reject.

    For the "batch update fails" test: stub tooling.query to resolve with records, stub tooling.update to reject; assert the function rejects with an error message that includes "Failed to expire overlapping traces".

    Import sinon at the top if not already imported in this file.
  </action>
  <verify>
    <automated>npx mocha --require ts-node/register test/utils/trace-helper.test.ts --grep "detectAndExpireOverlappingTraces" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/utils/trace-helper.test.ts contains a describe block with heading "detectAndExpireOverlappingTraces"
    - File contains at least 5 it() blocks within that describe block (one per behavior contract above)
    - Running the mocha command with --grep "detectAndExpireOverlappingTraces" shows all 5 tests in results (they will FAIL RED before Task 2 — that is expected and correct for TDD)
    - No syntax errors: npx tsc --noEmit on the test file exits 0 (type errors for the missing import are acceptable until Task 2)
  </acceptance_criteria>
  <done>Five failing tests define the behavior contracts for detectAndExpireOverlappingTraces(). RED phase confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detectAndExpireOverlappingTraces() in trace-helper.ts (GREEN phase)</name>
  <files>src/utils/trace-helper.ts</files>
  <read_first>
    - src/utils/trace-helper.ts — read the full file; specifically study checkExistingTraceFlag() (lines 145-186) for the existing query pattern (escapeSoql, tooling.query, ISO timestamp, tooling.update single-record pattern) — the new function extends and generalizes this pattern
    - src/types/trace.ts — confirm stoppedTraces element type {id: string; expirationDate: string} (from Plan 01 Task 1)
    - .planning/phases/05-core-detection-batch-expiration/05-RESEARCH.md — Pattern 1 (overlap detection query), Pattern 3 (multi-record update), Anti-Patterns section, Common Pitfalls section
    - .planning/phases/05-core-detection-batch-expiration/05-CONTEXT.md — D-01 through D-13 locked decisions
  </read_first>
  <behavior>
    - No overlap (empty query result) → return []
    - Single trace found → call tooling.update once with array of one record → return [{id, expirationDate}]
    - Multiple traces (3+) found → call tooling.update once with array of N records (NOT in a loop) → return array of N stopped traces
    - Query failure (connection error) → catch error, do not re-throw, return [] (per D-13)
    - Batch update failure → re-throw as Error("Failed to expire overlapping traces: {original message}") (per D-12)
  </behavior>
  <action>
    Add the exported async function detectAndExpireOverlappingTraces(org: Org, userId: string) returning Promise<Array<{id: string; expirationDate: string}>> to src/utils/trace-helper.ts.

    Place it immediately after the checkExistingTraceFlag() function (around line 187) so related functions are grouped together.

    Implementation steps (in order, matching the behavior contracts):
    1. Call org.getConnection() to get connection. Capture const now = new Date().toISOString().
    2. Wrap the query in try-catch. Query: SELECT Id, ExpirationDate FROM TraceFlag WHERE TracedEntityId = '{escapeSoql(userId)}' AND ExpirationDate > {now} — note: ExpirationDate comparison does NOT use quotes around the ISO timestamp in SOQL (consistent with existing pattern in trace-helper.ts line 156).
    3. If result.records.length === 0, return [].
    4. Build updates array: result.records.map(r => ({ Id: r.Id, ExpirationDate: now })). Type the records as Array<{Id: string; ExpirationDate: string}>.
    5. Attempt tooling.update('TraceFlag', updates) in a try-catch. If it rejects, throw new Error('Failed to expire overlapping traces: ' + original message).
    6. Return result.records.map(r => ({ id: r.Id, expirationDate: r.ExpirationDate })). Use the original ExpirationDate from the query result (not now), so callers can display the original expiry time.
    7. If the query in step 2 throws (catch block), return [] without re-throwing (per D-13).

    Add a JSDoc block with @param org, @param userId, @returns description, and a @throws note for batch update failure.

    Export the function (add to the existing named export pattern — do NOT use default export).
  </action>
  <verify>
    <automated>npx mocha --require ts-node/register test/utils/trace-helper.test.ts --grep "detectAndExpireOverlappingTraces" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/trace-helper.ts exports "detectAndExpireOverlappingTraces" (grep returns a match on that exact string as an exported function declaration)
    - All 5 mocha tests in the "detectAndExpireOverlappingTraces" describe block pass (GREEN phase)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - tooling.update is called with the string 'TraceFlag' as the first argument and an ARRAY as the second argument (not a single object) — confirmed by the "multiple overlaps" sinon calledOnce assertion passing
    - Query uses ExpirationDate > {now} without wrapping now in quotes (consistent with checkExistingTraceFlag pattern in existing codebase)
    - Function does not throw on query failure (query-fails test passes)
    - Function throws on update failure (update-fails test passes)
  </acceptance_criteria>
  <done>All 5 unit tests GREEN. TypeScript compiles cleanly. detectAndExpireOverlappingTraces() is exported and ready for Plan 03 to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userId → SOQL query | userId comes from --user-id flag or interactive search; must be sanitized before embedding in SOQL |
| Tooling API response → update payload | Record IDs from query result used directly in update call; must originate from Salesforce (not user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | detectAndExpireOverlappingTraces SOQL | mitigate | Use escapeSoql(userId) when interpolating userId into WHERE clause — same pattern as checkExistingTraceFlag line 156 |
| T-05-03 | Tampering | Batch update payload | accept | Record IDs in update array are fetched from Salesforce Tooling API response, not from user input; Salesforce enforces permissions on update |
| T-05-04 | Denial of Service | Batch update with large record array | accept | Typical overlap is 1-5 traces per D-02; Salesforce SObject Collection API limit is 200/call; no pagination needed |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- npx mocha --require ts-node/register test/utils/trace-helper.test.ts --grep "detectAndExpireOverlappingTraces" — all 5 tests pass
- npx tsc --noEmit exits 0
- grep "export.*detectAndExpireOverlappingTraces" src/utils/trace-helper.ts returns a match
- npm test exits 0 (full test suite, no regressions)
</verification>

<success_criteria>
- detectAndExpireOverlappingTraces() exported from src/utils/trace-helper.ts
- Returns [] when no active traces found
- Returns array of {id, expirationDate} when traces found and expired
- Calls tooling.update with array argument (batch, not sequential)
- Does not throw on detection query failure (returns [])
- Throws descriptive error on batch update failure
- All 5 unit tests pass GREEN
- Full test suite (npm test) exits 0
</success_criteria>

<output>
Create `.planning/phases/05-core-detection-batch-expiration/05-02-SUMMARY.md` when done
</output>
