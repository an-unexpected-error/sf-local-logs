---
phase: quick
plan: 260531-u75
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/trace-helper.ts
autonomous: true
requirements:
  - fix-traceflag-starttime
must_haves:
  truths:
    - "TraceFlag creation succeeds without passing the read-only StartTime field"
    - "ExpirationDate is still passed correctly (required writable field)"
    - "All existing unit tests pass"
  artifacts:
    - path: "src/utils/trace-helper.ts"
      provides: "createTraceFlag without StartTime in tooling.create payload"
      contains: "// StartTime is read-only"
  key_links:
    - from: "src/utils/trace-helper.ts createTraceFlag()"
      to: "Salesforce Tooling API TraceFlag"
      via: "connection.tooling.create"
      pattern: "tooling\\.create.*TraceFlag"
---

<objective>
Remove the read-only StartTime field from the TraceFlag creation payload in createTraceFlag().

Purpose: The Salesforce Tooling API rejects TraceFlag creation with "No such column 'StartTime'" because StartTime is a read-only field set automatically by Salesforce. Only TracedEntityId, DebugLevelId, and ExpirationDate are writable.

Output: src/utils/trace-helper.ts with StartTime removed from the tooling.create call, a comment explaining the omission, and all tests passing.
</objective>

<execution_context>
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mc/Repos/sf-local-logs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mc/Repos/sf-local-logs/src/utils/trace-helper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove StartTime from TraceFlag creation payload</name>
  <files>src/utils/trace-helper.ts</files>
  <action>
    In createTraceFlag() at lines 96-101, remove the `StartTime: startTime.toISOString()` line from the object passed to `connection.tooling.create('TraceFlag', {...})`.

    The StartTime field is read-only on the Salesforce TraceFlag sObject — it is set automatically by Salesforce upon creation and cannot be provided by the caller. Passing it causes: "No such column 'StartTime' on sobject of type TraceFlag".

    The local `startTime` variable (line 93) is used only to compute `expirationDate` (line 94). After this change the variable is still needed for that calculation, so keep it. The final payload must contain exactly three fields: TracedEntityId, DebugLevelId, ExpirationDate.

    Add a single inline comment immediately after the closing brace of the create call (or as a trailing comment on the last field line) such as:
    `// StartTime is read-only on TraceFlag; Salesforce sets it automatically`

    Do not change any other logic in createTraceFlag() or anywhere else in the file.
  </action>
  <verify>
    <automated>cd /Users/mc/Repos/sf-local-logs && yarn test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `StartTime` does not appear in the tooling.create payload
    - `ExpirationDate` is still passed
    - `yarn test` exits 0 with no failures
    - A comment explains that StartTime is read-only
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Salesforce Tooling API | Authenticated org connection; only fields accepted by the API schema may be sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u75-01 | Tampering | tooling.create payload | accept | Fix removes the offending field; remaining fields (TracedEntityId, DebugLevelId, ExpirationDate) are all required and schema-validated by Salesforce |
</threat_model>

<verification>
Run the full test suite: `cd /Users/mc/Repos/sf-local-logs && yarn test`

Confirm:
- No test references `StartTime` in a `tooling.create` call assertion
- `createTraceFlag` unit path (if exercised) passes
- No TypeScript compilation errors: `yarn tsc --noEmit`
</verification>

<success_criteria>
TraceFlag creation no longer errors with "No such column 'StartTime'". The tooling.create payload contains only TracedEntityId, DebugLevelId, and ExpirationDate. All unit tests pass.
</success_criteria>

<output>
Create `.planning/quick/260531-u75-fix-traceflag-creation-error-1-no-such-c/260531-u75-SUMMARY.md` when done.
</output>
